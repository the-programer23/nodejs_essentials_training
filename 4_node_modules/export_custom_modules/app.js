// const counter = require('./myModule');

// counter.incCount();
// counter.incCount();

// console.log(counter.getCount());

const { incCount, decCount, getCount} = require('./myModule');

incCount();
incCount();

console.log(getCount());